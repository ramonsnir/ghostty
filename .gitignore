*~
.*.swp
.swp
*.log
.DS_Store
.vscode/
.direnv/
.envrc.local
.flatpak-builder/
zig-cache/
.zig-cache/
zig-out/
/zig-out
build-cmake/
CMakeCache.txt
CMakeFiles/
/build.zig.zon.bak
/result*
/.nixos-test-history
example/*.wasm
test/ghostty
test/cases/**/*.actual.png
flatpak/builddir/
flatpak/repo/

glad.zip
/Box_test.ppm
/Box_test_diff.ppm
/ghostty.qcow2

vgcore.*

# fork: .claude/ is local-only working state (plans, workflows, per-task build
# worktrees). Nothing under it is tracked — feature docs live at the repo root
# (PTYHOST.md, AGENT-DASHBOARD.md, …). Force-add (`git add -f`) if a file ever
# genuinely needs tracking.
.claude/

